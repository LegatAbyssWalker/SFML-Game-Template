#ifndef MANAGEREXAMPLE_H
#define MANAGEREXAMPLE_H

#include "State.h"

#include <iostream>
#include <array>

class ManagerExample {
	/*
	public:
		CactiManager();
		
		void renderTo(sf::RenderWindow& window);
		void update();
		void addCacti(unsigned int textureType);
		
		bool collision(sf::FloatRect bounds);

	private:
		std::vector<std::unique_ptr<Cacti>> cactiVector;
		std::array<sf::Texture, 3> cactiTexture;
	
	*/
};

#endif